import { Request, Response} from "express";
import User from "../entity/User";
import { getRepository } from "typeorm";

class UserController {
    static queryUserInfo = async (req: Request, res: Response) => {
        try {

        } catch (e) {
            
        }
    }
}   

export default UserController